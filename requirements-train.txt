# Training / export / evaluation dependencies for phoneanalyzer.
# Builds on the inference deps — install both with:  pip install -r requirements-train.txt
-r requirements.txt

# RF-DETR object detector — training & ONNX export
# (detection_model/training/train.py, detection_model/training/export.py)
rfdetr[train,loggers,metrics]

# Detection evaluation metrics (detection_model/training/test.py)
torchmetrics
tqdm

# Experiment tracking (detection_model/training/train.py)
wandb

# Crack classifier training (crack_detection/training/cracks.py)
scikit-learn
matplotlib
seaborn
